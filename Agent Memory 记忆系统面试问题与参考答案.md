# Agent Memory 记忆系统面试问题与参考答案

# Agent Memory 简历项目讲解与面试问答



## 简历内容



> 设计四层语义金字塔长期记忆（L0 原始对话 → L1 原子事实 → L2 场景聚类 → L3 用户画像），实现 L3 画像定向、逐层下钻的可追溯检索；采用 Mermaid 符号画布压缩短期记忆中的长任务中间输出，兼顾解析精确度与可读性；设计 PreferenceAgent 智能识别追加/覆盖偏好动作，异步 LLM 总结机制实现会话信息向长期记忆的自动沉淀。
> 
> 



## 一、四层语义金字塔长期记忆



### 1\. 为什么设计四层记忆？



原始对话会不断增长，而且临时要求、长期偏好和历史事件混在一起。如果每次把全部历史交给模型，不仅 Token 成本高，关键信息还容易被噪声淹没。



因此系统按照信息粒度和稳定性设计四层记忆：



|层级|内容|差旅示例|作用|
|---|---|---|---|
|L0 原始对话|用户和 Agent 的原始消息|“我家在上海，以后坐飞机尽量靠窗”|保留原始证据|
|L1 原子事实|单一、可独立检索的事实|常住地=上海；座位偏好=靠窗|精确检索和更新|
|L2 场景聚类|按项目或事件组织相关事实|“2026 年 8 月北京出差”|保存完整业务场景|
|L3 用户画像|多条长期记忆形成的稳定总结|常驻上海、经常商务出差|提供全局用户背景|



层级越高，信息越抽象、稳定、精简；层级越低，细节越完整、可追溯性越强。



### 2\. 什么是“L3 画像定向、逐层下钻”？



系统先通过 L3 判断用户的长期特征和问题方向，再到 L2 定位相关场景，然后从 L1 读取具体事实；如果需要核验，最后下钻到 L0 查看原始对话。



```Plain Text
flowchart TD
    Q["用户：上次去北京住的哪家酒店？"]
    L3["L3 用户画像<br/>识别为差旅历史问题"]
    L2["L2 场景<br/>定位最近一次北京出差"]
    L1["L1 原子事实<br/>读取酒店和入住时间"]
    L0["L0 原始对话<br/>核对预订记录"]
    A["生成回答"]

    Q --> L3 --> L2 --> L1 --> A
    L1 -.需要验证.-> L0 --> A
```



“画像定向”减少无关搜索；“逐层下钻”按需加载细节；“可追溯”表示上层总结最终可以回到 L0 原始证据。



### 3\. 为什么不直接向量检索全部对话？



向量相似只能说明语义接近，不能保证时间、人物和场景正确。例如搜索“北京酒店”可能召回多次不同行程。分层检索先确定用户和场景，再寻找具体事实，可以减少语义相似但业务无关的结果。



向量检索仍可用于 L1/L0 搜索，并与 BM25、RRF 结合：BM25 负责地名、航班号等精确关键词，向量检索负责同义表达，RRF 融合两种排序。



### 4\. 四层记忆如何自动生成？



一次对话完成后，系统先增量写入 L0，再通过后台流水线逐层加工：



```Plain Text
L0：记录原始对话
 ↓
L1：提取偏好、事实、约束和事件
 ↓
L2：聚合到对应行程或项目场景
 ↓
L3：低频总结稳定的用户画像
```



L1 可按消息数量或会话空闲触发，L2 在 L1 更新后继续聚合，L3 更新频率最低，避免一次临时行为导致画像频繁变化。



### 5\. 如何处理临时约束与长期偏好？



例如用户说：



> “以前喜欢住汉庭，这次客户安排住希尔顿，以后还是优先汉庭。”
> 
> 



- “这次住希尔顿”是当前行程约束，进入 L2。

- “以后优先汉庭”是长期偏好，进入 L1。

- L3 可总结为“通常优先经济型酒店，客户指定时接受其他酒店”。

    

系统通过时间、作用域和来源区分两种信息，不会用一次临时安排覆盖长期偏好。



## 二、Mermaid 符号画布压缩短期记忆



### 1\. 它解决什么问题？



长任务会产生大量工具调用、查询结果和中间结论。全部保留在上下文中会快速消耗 Token，也会让模型难以判断任务执行到了哪一步。



系统将线性的中间输出压缩为 Mermaid 任务画布，只保留任务阶段、节点状态、依赖关系和关键结论：



```Plain Text
flowchart TD
    A["需求分析<br/>status: done<br/>确认日期和预算"]
    B["航班查询<br/>status: done<br/>得到 3 个候选"]
    C["酒店查询<br/>status: done<br/>筛选公司附近酒店"]
    D["行程规划<br/>status: doing<br/>组合航班和酒店"]
    E["用户确认<br/>status: todo"]

    A --> B
    A --> C
    B --> D
    C --> D
    D --> E
```



下一轮模型读取画布，就能恢复任务结构和当前进度，不必重新阅读所有工具输出。



### 2\. 为什么使用 Mermaid，而不是普通摘要？



普通摘要适合描述内容，但不擅长稳定表达分支、依赖、失败重试和任务状态。Mermaid 有固定的节点与连线语法，对模型容易解析，对开发者又可以直接渲染查看。



所以这里的 Mermaid 不只是可视化图，而是一种结构化、可压缩的短期工作记忆。



### 3\. 如何兼顾解析精确度与可读性？



系统使用三部分配合：



|结构|作用|
|---|---|
|Mermaid 画布|保存任务结构、状态、依赖和摘要|
|node\_mapping|将 tool\_call\_id 精确映射到画布节点|
|refs/\{tool\_call\_id\}\.md|保存完整的原始工具输出|



Mermaid 负责“看懂任务”，node\_mapping 负责“准确定位”，refs 负责“保留证据”。系统压缩的是默认上下文，而不是删除原始数据；需要细节时，可以从节点回读对应工具输出。



### 4\. 什么时候更新画布？



系统先判断当前工作是否属于长任务。目标一致时持续更新活动画布；目标明显变化时创建新画布，将旧画布放入历史。上下文压力升高后，系统用活动画布替换大段中间输出，已完成的早期步骤还可以折叠成里程碑节点。



## 三、PreferenceAgent 偏好识别



### 1\. PreferenceAgent 做什么？



PreferenceAgent 从自然语言中识别用户偏好，并输出结构化的偏好类型、值和操作动作。



例如用户说：



> “我平时喜欢住汉庭，以后全季也可以。”
> 
> 



Agent 输出：



```JSON
{
  "preferences": [
    {
      "type": "hotel_brands",
      "value": "全季",
      "action": "append"
    }
  ]
}
```



PreferenceAgent 不直接修改数据库，而是返回结构化动作，再由 OrchestrationAgent 校验并持久化。这样可以把偏好识别和数据写入解耦。



### 2\. 为什么区分追加和覆盖？



- append：适用于可以同时存在多个值的偏好，例如酒店品牌和航空公司。

- replace：适用于通常只有一个当前值的偏好，例如常住城市和默认出发地。

    

例如：



```Plain Text
“我也可以坐东航” → airlines 追加“东航”
“我搬到杭州了”   → home_location 覆盖为“杭州”
```



如果全部覆盖，会丢失多个可接受选项；如果全部追加，又会保留已经失效或冲突的旧值。



### 3\. PreferenceAgent 的数据流是什么？



```Plain Text
flowchart LR
    U["用户输入"]
    P["读取已有偏好"]
    PA["PreferenceAgent<br/>识别 type/value/action"]
    O["OrchestrationAgent<br/>校验并执行动作"]
    M["长期记忆"]
    A["差旅 Agent 使用偏好"]

    U --> P --> PA --> O --> M --> A
```



Agent 判断动作前需要读取已有偏好。比如用户已经偏好汉庭，又说“全季也可以”，结合旧数据才能判断为追加而不是覆盖。



### 4\. 如何避免临时需求污染长期偏好？



系统识别语句中的时间和作用域。“这次帮我订希尔顿”属于当前行程，不进入长期偏好；“以后优先订希尔顿”才进入长期记忆。还可以结合重复出现次数和用户确认提高可靠性。



## 四、异步 LLM 总结与自动沉淀



### 1\. 为什么采用异步总结？



历史对话会不断增长，业务 Agent 没必要每次读取几十轮完整消息。系统在后台调用 LLM，将历史聊天、结构化偏好和行程记录总结成简短的长期背景，供以后会话使用。



总结需要额外模型调用，如果放在主链路会增加响应时间。异步处理不会阻塞当前回答；即使总结失败，原始对话和结构化偏好仍然存在，后台可以重试。



### 2\. 会话信息如何沉淀为长期记忆？



```Plain Text
flowchart TD
    U["用户与差旅助手对话"]
    S["短期记忆<br/>保存最近若干轮"]
    H["长期 chat_history"]
    P["PreferenceAgent<br/>提取结构化偏好"]
    T["行程完成<br/>保存 trip_history"]
    L["异步 LLM 总结"]
    C["新会话上下文<br/>偏好 + 历史摘要 + 相关行程"]

    U --> S
    U --> H
    U --> P --> H
    U --> T --> H
    H --> L --> C
```



当前会话依赖短期记忆；每轮消息同时追加到长期 chat\_history。PreferenceAgent 产生结构化偏好，行程完成后保存 trip\_history。异步 LLM 再从历史信息中提炼用户习惯和经历，实现从会话信息到长期记忆的自动沉淀。



### 3\. 为什么既要结构化偏好，又要 LLM 摘要？



结构化偏好适合程序直接使用，例如筛选酒店品牌、推断默认出发地；LLM 摘要适合表达难以拆成固定字段的背景，例如“经常从上海出发进行两到三天的商务出差，更关注交通便利性”。



业务 Agent 优先使用结构化字段保证准确性，同时利用摘要理解完整背景。



## 五、完整差旅案例



用户第一次说：



> “我家在上海，平时喜欢住汉庭，坐飞机尽量靠窗。”
> 
> 



处理过程：



1. 原始消息写入 L0 和长期 chat\_history。

2. PreferenceAgent 将常住地识别为 replace，将酒店品牌识别为 append，将座位偏好识别为 replace。

3. OrchestrationAgent 执行对应动作，形成 L1 原子偏好。

4. 异步 LLM 根据历史生成长期背景，逐步形成 L3 画像。

    

下一次用户说：



> “下周二帮我安排去北京出差，当天开完会，周三回来。”
> 
> 



处理过程：



1. L3 判断这是以上海为常用出发地的商务差旅用户。

2. 系统下钻 L1，获得上海、汉庭、靠窗等具体偏好。

3. 创建“北京出差”L2 场景，保存日期、目的地和执行状态。

4. 航班、酒店和会议查询的中间输出被压缩到 Mermaid 画布。

5. 规划 Agent 根据偏好生成行程。

6. 行程完成后写入 trip\_history，并异步沉淀为新的长期背景。

    

## 六、面试官可能追问



### 1\. 这套记忆系统最大的难点是什么？



难点不是保存数据，而是判断哪些信息值得长期保存，并区分临时约束、长期偏好和历史事实。另一个难点是压缩后仍然保持可追溯，避免模型总结错误成为无法验证的长期事实。



### 2\. 四层记忆是不是过度设计？



单轮问答不需要四层记忆，但跨会话和长期任务中，原始证据、事实、场景和画像具有不同生命周期。分层后可以分别设置更新频率和召回方式，降低 Token 成本并提高检索准确性。



### 3\. Mermaid 压缩会不会丢信息？



画布会省略低价值细节，但完整工具输出仍保存在 refs 中。node\_mapping 保存节点与原始输出的对应关系，因此需要时可以回读。系统压缩的是模型默认携带的上下文，不是原始证据。



### 4\. 为什么不让 PreferenceAgent 直接写数据库？



LLM 只负责理解和输出结构化动作，编排层负责校验和执行。这样便于统一处理字段类型、追加/覆盖规则、权限、审计和失败重试，也避免模型错误直接污染长期记忆。



### 5\. 异步总结失败怎么办？



失败不阻塞当前回答。原始聊天和结构化偏好已经保存，后台任务可以重试；总结长期不可用时，系统仍可依赖短期上下文和结构化偏好降级运行。



### 6\. 如何评价系统效果？



主要指标包括跨会话问题正确率、偏好采用率、错误偏好写入率、用户重复提供信息的次数、平均上下文 Token、长任务状态恢复准确率、任务完成率和响应延迟。



## 七、一分钟面试表达



> 我主要负责长期记忆和长任务短期记忆两部分。长期记忆采用 L0 到 L3 的四层语义金字塔：L0 保存原始对话作为证据，L1 提取可独立检索的偏好和事实，L2 按行程或项目聚合场景，L3 形成稳定用户画像。检索时先用 L3 确定方向，再逐层下钻到 L2、L1，必要时回到 L0 验证，因此既节省上下文又具有可追溯性。对于长任务，我把大量工具输出压缩为 Mermaid 任务画布，用节点和边保留阶段、依赖和状态，再通过 node\_mapping 和 refs 保留与原始输出的映射，兼顾模型解析和人工阅读。在差旅助手中，我还设计了 PreferenceAgent，将自然语言偏好识别为 append 或 replace 动作，由编排层统一落库；会话结束后异步调用 LLM 汇总历史对话、偏好和行程，让短期信息自动沉淀为长期记忆。
> 
> 



