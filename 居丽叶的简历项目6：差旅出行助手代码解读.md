# 居丽叶的简历项目6：差旅出行助手代码解读

面试文档见：[居丽叶的简历项目6：差旅出行助手](https://my.feishu.cn/wiki/Qm5NwLIM2iF4GwkulSpcbdSynvh)

# 项目运行

## 安装依赖

```Bash
python3.13 -m venv venv
source venv/bin/activate   *# macOS/Linux*
*# venv\Scripts\activate    # Windows*

pip install -r requirements.txt
```

## 配置 LLM

编辑项目根目录下的 `config.py`，填写你的模型 API 信息：

```Python
LLM_CONFIG = {
    "model_name": "模型",
    "base_url": "你的 Base URL",
    "api_key": "你的 API Key",  
}
```

## 初始化知识库

知识库位于 \`\.claude/skills/ask\-question/data/documents/\`，内含 8 类文档：差旅规定、报销规定、预订指南、FAQ、应急指南、平台指南、城市指南、环保倡议。当用户询问这些场景下的政策或流程时，系统会通过 **RAG 智能体**进行检索并回答。使用前需先运行初始化脚本，该脚本会自动对长文档进行切分（Chunking）并建立向量索引：

- 如果是在windows环境运行项目，安装milvus参考https://milvus\.io/docs/zh/v2\.5\.x/install\_standalone\-windows\.md。或者切换到linux环境运行项目。

```Python
python .claude/skills/ask-question/script/init_knowledge_base.py
```

## 启动

在项目根目录执行：

```Bash
python cli.py
```

按提示输入用户 ID（可直接回车用默认），进入后输入自然语言即可（如「北京住宿标准是多少」「规划从上海到北京的行程」）。

## **在 Claude 里用（Skills）**

本项目在 **Claude**里也可用，在对话里用自然语言提问即可，Claude 会按意图调用对应的 Agent。

> claude安装详见https://t\.zsxq\.com/I0RCJ
> 
> 

- **Skills 目录**：\`\.claude/skills/\`（知识库问答、天气/搜索、行程规划、记忆查询、偏好管理）

- **怎么问**：直接说需求，例如「北京的住宿标准是多少」「帮我规划从上海到北京的行程」「北京明天天气怎么样」。或者直接指定skills，例如「 /plan\-trip  我从3月11日从北京出发，在杭州出差一周，3月18日返回北京，帮我安排行程 」

# 关键代码

![image\.png](图片和附件/image.png)

整个项目的流程为：

1. **用户输入**

2. **`IntentionAgent`****（意图识别）**：语义理解意图、识别实体、生成调度计划与优先级

3. **`OrchestrationAgent`****（协调器）**：按优先级调度子智能体，同优先级并行，集成记忆

    1. **优先级 1（并行）**  

    - `MemoryQuery`（记忆查询）  

    - `EventCollection`（事项收集）  

    - `Preference`（偏好管理）  

    - `InformationQuery`（信息查询 / 天气、网络搜索）  

    - `RAGKnowledgeAgent`（知识库问答）

    2. **优先级 2**  

    - `ItineraryPlanningAgent`（行程规划，依赖优先级 1 结果）

4. **结果聚合与记忆更新**：聚合结果、写长期记忆、生成回复

5. **用户看到结果**

代码入口在 **cli\.py** 的 `process_query()`函数。

## 项目入口主函数（cli\.py）

- `initialize_system`：负责初始化系统、模型`OpenAIChatModel`、记忆管理`MemoryManager`、意图识别agent`IntentionAgent`、规划agent`OrchestrationAgent`、熔断器`CircuitBreaker`、懒加载智能体注册器（其余Agent只有被调用时才会加载，避免项目一启动时加载全部Agent，时间过长）`LazyAgentRegistry`。

- `process_query`：

1. 首先设置熔断策略：请求前检查熔断器；若已打开则直接提示「服务暂时不可用」并 return，不调用 LLM。连续失败达阈值后打开，持续一段时间后进入半开试探，半开连续成功若干次后自动关闭恢复。

    ```Python
    if self.circuit_breaker:
        try:
            self.circuit_breaker.raise_if_open()
        except CircuitOpenError:
            self.console.print(
                "\n[bold yellow]⚠ 服务暂时不可用，请稍后再试。[/bold yellow]\n",
                style="dim"
            )
            return
    
    rc = RESILIENCE_CONFIG
    max_retries = rc.get("max_retries", 3)
    ```

2. 上下文构建： `_get_long_term_summary()` 获取长期记忆摘要，和短期最近 5 轮对话一起拼成消息列表。 

    ```Python
    long_term_summary = await self._get_long_term_summary(user_input)
    recent_context = self.memory_manager.short_term.get_recent_context(n_turns=5)
    context_messages = []
    if long_term_summary:
        context_messages.append(Msg(name="system", content=long_term_summary, role="system"))
    for msg in recent_context:
        context_messages.append(Msg(name=msg["role"], content=msg["content"], role=msg["role"]))
    context_messages.append(Msg(name="user", content=user_input, role="user"))
    ```

3. 调用意图识别Agent，解析返回 JSON

    ```Python
    intention_result = None
    try:
        intention_result = await retry_with_backoff(
            **lambda**: self.intention_agent.reply(context_messages),
            max_retries=max_retries,
            base_delay_sec=rc.get("retry_base_delay_sec", 1.0),
            max_delay_sec=rc.get("retry_max_delay_sec", 30.0),
        )
        if self.circuit_breaker:
            self.circuit_breaker.record_success()
    except CircuitOpenError:
        raise
    except Exception as e:
        if self.circuit_breaker:
            self.circuit_breaker.record_failure()
        raise
    
    *# 3. 解析意图识别结果（原逻辑不变：解析失败则友好提示并 return）*
    try:
        intention_data = json.loads(intention_result.content)
    except json.JSONDecodeError:
        self.console.print("❌ 无法理解您的需求，请重新描述", style="bold red")
        return
    ```

4. 添加用户输入到短期记忆

    ```Python
    memory_manager.add_message("user", user_input)
    ```

5. 调用调度Agent

    ```Python
    orchestration_result = await retry_with_backoff(
        **lambda**: self.orchestrator.reply(intention_result),
        max_retries=max_retries,
        base_delay_sec=rc.get("retry_base_delay_sec", 1.0),
        max_delay_sec=rc.get("retry_max_delay_sec", 30.0),
    )
    ```

6. 解析结果并展示，将模型回复添加到短期记忆

    ```Python
    try:
        result_data = json.loads(orchestration_result.content)
    except json.JSONDecodeError:
        result_data = {"error": "解析结果失败"}
    
    *# 7. 显示调用的智能体与最终结果（原逻辑不变）*
    self._display_agents_called(result_data)
    self.console.print()
    self._display_results(result_data)
    self.memory_manager.add_message("assistant", json.dumps(result_data, ensure_ascii=False))
    ```

- 长期记忆总结`_get_long_term_summary`：加入用户偏好信息，使用LLM总结历史聊天记录，再根据目的地和出发地筛选相关聊天，加入上下文。

## 意图识别Agent（intention\_agent\.py）

- **职责**：根据当前用户输入 \+ 系统记忆 \+ 对话历史，输出**结构化调度计划**，不直接执行具体业务。

- **核心逻辑**：

    - 从消息列表拆出「系统记忆」和「对话历史」，拼成 prompt 的上下文。

    - 调用 `SkillLoader` 动态加载所有 Skill 的 metadata，注入 Prompt，实现 **渐进式披露**

    - 输出 JSON 中必须包含 **agent\_schedule列表**，每项为 `agent_name`、`priority`、`reason`、`expected_output` 等。还包含推理过程、意图类型、关键实体、改写后的query等，供下游使用。

## 调度Agent（orchestration\_agent\.py）

- **职责**：按意图结果调度子智能体，同优先级并行，不同优先级顺序执行，并聚合结果、回写记忆。

- **核心逻辑**：

    - 读取 IntentionAgent 的调度决策的 **agent\_schedule，**按 **priority** 排序，同一 priority 的 task 一组。

    - `_prepare_context()`：读取推理过程、意图类型、关键实体、改写后的query，从 记忆模块memory\_manager 取 最近的对话、用户偏好，组成上下文。

    - 对每一组 task 调用 `_execute_parallel_agents()`：对每个 task 调用 `_execute_agent()`，同组内用 `asyncio.gather` 并行。

    - `_execute_agent`：从 `agent_registry[agent_name]` 取智能体（这里 registry 是 LazyAgentRegistry 懒加载得到的）。

    - `_aggregate_results()`：把各 agent 的 result 聚合。

    - `_update_memory()`：根据结果更新长期记忆（例如 preference 的写入、行程历史的写入等）。

## **子智能体注册（**lazy\_agent\_registry\.py）

- **作用**：实现了**插件化架构**。扫描 `.claude/skills` 目录，动态发现并加载子智能体。

- **核心机制**：

    - `_discover_skills()`：遍历 `.claude/skills/`，建立 `skill_name` 到 `script_path` 的映射。

    - `__getitem__(agent_name)`：

        - 首次访问时，使用 `importlib.util` 动态加载对应的 `agent.py` 模块。

        - 反射查找模块中继承自 `AgentBase` 的类。

        - 实例化 Agent，并自动注入 `memory_manager`（如果构造函数需要）。

        - 存入 `cache`，下次直接返回实例。

## 记忆管理（context目录）

- **职责**：统一封装「短期记忆」和「长期记忆」

- **短期记忆**：`ShortTermMemory()`，存储当前会话中的最近 N 轮对话。

- **长期记忆**：`LongTermMemory()`，持久化到 `data/memory/{user_id}.json`，存偏好、行程历史、全量聊天记录等；可以跨会话。

- `add_message()`：将对话内容写入短期和长期记忆。

- `get_context_for_agent()`：拼成给 Agent 的上下文（长期记忆摘要 \+ 用户偏好 \+ 短期对话记忆）。

- `get_long_term_summary_async()`：用 LLM 对长期聊天做摘要，供意图识别时使用。

## Skills

所有子智能体均位于 `.claude/skills/{skill-name}/script/agent.py`，实现完全的自包含。

- **统一入参**：orchestration agent传入的 `Msg` 的 content 为 JSON：`{"context": {...}, "reason": "...", "expected_output": "...", "previous_results": [...]}`。

    - **context** 中含 `rewritten_query`、`user_preferences`、`recent_dialogue` 等。

    - **previous\_results** 为前面已执行 agent 的 result 列表（如 event\_collection 的输出给 itinerary\_planning 用）。

- **统一返回**：各 agent 的 `reply()` 返回 `Msg`，`content` 一般为 JSON 字符串（或可被解析为 dict），编排器会 `json.loads` 后聚合、写记忆。

Skills介绍

1. MemoryQueryAgent \(记忆查询智能体\)

- **职责**: 查询用户的历史记忆

- **查询内容**:

    - 旅行历史（trip\_history）

    - 用户偏好（preferences）

    - 历史对话摘要（chat\_history）

- **特点**:

    - 直接查询本地记忆，无需联网

    - 使用 LLM 生成自然语言回答

    - 支持复杂的记忆推理

- **示例**: "我过去去过哪些地方？"、"我上次去北京是什么时候？"

2. EventCollectionAgent \(事项收集智能体\)

- **职责**: 收集行程规划的核心信息

- **收集内容**: 出发地、目的地、出发时间、返程时间、出行目的

- **特点**: 主动推断缺失信息

3. PreferenceAgent \(偏好管理智能体\)

- **职责**: 识别和管理用户所有偏好

- **管理偏好**:

    - 酒店品牌、航空公司、座位偏好、房型偏好

    - 机型偏好、餐饮偏好、交通偏好、预算等级

    - 支持任意自定义偏好类型

- **智能模式**:

    - **追加模式**：识别「还」、「也」等关键词，追加到现有偏好

    - **覆盖模式**：识别「搬家到」、「改成」等关键词，替换旧偏好

    - **示例**: "我还喜欢汉庭" → 追加；"我搬家到上海" → 覆盖

- **特点**:

    - 感知当前已有偏好，避免重复

    - 所有偏好作为长期偏好持久化保存

    - 从对话中提取隐含偏好

4. InformationQueryAgent \(信息查询智能体\)

- **职责**: 实时信息检索（联网）

- **查询能力**: DuckDuckGo 搜索 \+ LLM 摘要

- **查询场景**: 天气、景点、实时新闻、通用问答

5. RAGKnowledgeAgent \(知识库查询智能体\)

- **职责**: 查询企业商旅知识库

- **技术栈**: Milvus Lite \+ BGE 中文向量模型

- **特点**: 提供文档溯源，返回参考来源

6. ItineraryPlanningAgent \(行程规划智能体\)

- **职责**: 生成完整行程计划

- **规划内容**: 每日时间表、住宿建议、餐饮建议、交通路线、注意事项

- **特点**: 即使信息不完整也给出合理建议

下面以 **ask\-question**（路径：`.claude/skills/ask-question`）为例，说明 SKILL\.md 与 agent\.py 的写法与对应关系。

Skill 根目录下的 `SKILL.md` 是给编排/触发逻辑和人类维护者看的说明，这个Skill包含：

- **Frontmatter（YAML 头）**  

    - `name: ask-question`：技能唯一标识。  

    - `description`：何时触发、做什么。例如「用户问差旅政策、报销、预订、城市信息等时使用；触发句式如『XX标准是多少』『如何XX』『XX怎么办』；使用 RAGKnowledgeAgent 从知识库检索并回答」。

- **When to Use**
列举典型触发场景，如「XX标准是多少」「如何报销」「航班延误怎么办」以及需要基于企业/项目知识文档回答时。

- **Agent**
标明实际执行者：`RAGKnowledgeAgent`（对应 `script/agent.py` 中的类）

- **初始化与调用**
示例代码：如何 `init_agentscope`、创建 `OpenAIChatModel`、从 `config.RAG_CONFIG` 读嵌入模型路径、实例化 `RAGKnowledgeAgent`（knowledge\_base\_path、collection\_name、top\_k 等），以及如何用 `Msg` 调用 `await rag_agent.reply(user_msg)` 并解析返回的 JSON。

- **返回格式**
约定编排器可依赖的字段：`status`（`"success"` / `"no_knowledge"`）、`answer`、`retrieved_documents`、`query`。

- **知识库**
说明向量库路径（如 `data/rag_knowledge/`、Milvus Lite）、源文档目录（如 `data/documents/`）及类别，以及首次使用前需执行 `python scripts/init_knowledge_base.py`。

- **回答生成指南（Progressive Disclosure）**
面向 LLM 的「回答要求」：严格基于知识库、不得编造、信息不足时明确说「知识库中没有相关信息」等。这部分会在 **agent\.py** 里通过 `SkillLoader` 动态读取并注入到 prompt，实现「说明在 MD、逻辑在代码」的 Progressive Disclosure。



`.claude/skills/ask-question/script/agent.py`该文件自包含 RAG 问答逻辑。

- **类与职责**  

    - **RAGKnowledgeAgent\(AgentBase\)**：基于向量库（Milvus Lite）做检索，再结合 LLM 生成答案。  

    - 技术栈：Milvus Lite、sentence\-transformers 做向量化、项目配置的 LLM 生成答案。

- **初始化 ****`init`**  

    - 参数：`name`、`model`、`knowledge_base_path`（默认 skill 下的 `data/rag_knowledge`）、`collection_name`、`embedding_model`、`top_k`。  

    - 从 **config\.RAG\_CONFIG** 读取 `embedding_model`（支持本地路径，避免连 HuggingFace）；若路径存在则本地加载，否则回退到如 `BAAI/bge-small-zh-v1.5`。  

    - 使用 **MilvusClient** 连接 `knowledge_base_path/milvus_lite.db`；若 collection 不存在则 **create\_collection**（dimension、COSINE、auto\_id=False）。  

    - 创建 **SkillLoader**，供后续从同 skill 的 `SKILL.md` 拉取「回答生成指南」。  

    - 依赖未安装或初始化失败时置 `self.initialized = False`，后续 `reply` 直接返回错误信息。

- **检索 ****`search_knowledge(query, top_k)`**  

    - 用 `embedding_model.encode(query)` 得到向量，在 Milvus 中 **search**，取 `output_fields=["id", "content", "metadata"]`。  

    - 返回列表，每项含 `id`、`content`、`metadata`、`distance`；异常时返回空列表。

- **主流程 ****`async reply(x)`**  

    1. **解析入参**：若 `x.content` 是 JSON 字符串（以 `{` 开头），则解析并从 `context.rewritten_query` 或顶层 `rewritten_query` 取出用户 query；否则整段 content 当作 `user_query`。

    2. **检索**：`retrieved_docs = self.search_knowledge(user_query)`。若无结果，直接返回 `status: "no_knowledge"`、`answer` 为「知识库中没有找到相关信息」的 JSON Msg。

    3. **拼 prompt**：将检索到的片段拼成「知识库信息」；通过 **skill\_loader\.get\_skill\_content\("ask\-question"\)** 读取 SKILL\.md 中的「回答生成指南」作为任务说明（Progressive Disclosure）；再加上「严格基于知识库、不得编造」等约束。

    4. **调用 LLM**：`response = await self.model(messages)`；兼容异步生成器或普通 content/text 属性，得到字符串 `answer`；若 LLM 误输出 JSON 外形则尝试从 `answer`/`content` 字段提取。

    5. **统一返回**：返回 `Msg`，`content` 为 JSON 字符串：`{status, query, answer, retrieved_documents}`；`retrieved_documents` 为摘要版（如 content 截断到 200 字）便于编排器展示。

编排器调用本 skill 时，只需传入统一格式的 `Msg`（含 `context.rewritten_query`），即可得到上述 JSON 结构的 `Msg.content`，无需关心 SKILL\.md 与 agent 内部实现细节；SKILL\.md 负责「何时用、返回啥、如何初始化」，agent\.py 负责「怎么检索、怎么调 LLM、怎么拼 prompt」。

## **连接与可用性（circuit\_breaker\.py \& llm\_resilience\.py）**

- `CircuitBreaker`：维护状态 closed / open / half\_open；连续失败达阈值则打开，一段时间后半开试探，成功次数达标后关闭；

- `retry_with_backoff()`：对传入的协程工厂做有限次重试，采用指数退避算法。

- `run_health_check()`：用最小请求探测 LLM 是否可达。















