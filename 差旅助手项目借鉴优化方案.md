# 差旅助手项目借鉴优化方案

> 汇总：对照 Claude Code / opencode / deepseek-harness 三家开源 harness 源码，梳理本项目可借鉴的全部优化点
> 结构：按项目五大模块组织（编排层 / 记忆系统 / RAG 检索 / 插件化 / 消息与缓存），每项含：现状 → 借鉴机制 → 落点 → 落地成本 → 面试话术
> 优先级标记：★★★ = 被问必挂的缺口，必须补；★★ = 高价值加分项；★ = 锦上添花

---

## 〇、总纲：借鉴方法论（面试开场白）

> "这个项目我系统性对照过三家开源 harness——Claude Code、opencode、deepseek-harness。借鉴遵循一个原则：**取机制不取形态**。判断一个机制值不值得借，看它解决的问题在我的场景是否存在，而不是它在大厂产品里是否先进。"

三条已验证的选型判据（都有出处）：
1. **确定性优先**：意图集合封闭（6 大类）时用确定性路由，不用模型自主路由——CC 压缩流水线"便宜操作先跑"、opencode mode 显式声明，同一哲学
2. **规划显式化程度匹配任务可预知度**：差旅任务短周期可分解 → Task 式派发 + 轻量计划，不上 Swarm
3. **易变内容远离缓存前缀**：三家共同纪律——规则进 system，正文进 user 侧

---

## 一、编排层优化

### 1.1 综合环节 ★★★

**现状**：Skill 返回结果后直接拼给用户，编排层是"路由管道"。

**借鉴机制**：CC coordinator prompt 铁律——"You never hand off understanding to another worker"。调度器必须自己读懂子任务结果，消化后写成带具体细节的下一步指令；禁止 "based on your findings, fix it" 式懒派发。

**落点**：OrchestrationAgent 在 Skill 返回后增加一次"读懂→重组"：
- 单 Skill 结果：校验完整性 + 格式化后输出
- 多 Skill 协作（查天气→排行程）：前一个 Skill 的结果消化成后一个 Skill 的具体输入，而不是原样转发

**落地成本**：编排层一次 LLM 调用 + prompt 模板。

**面试话术**："我的编排层不只是分发，还有综合职责——子智能体的原始返回必须经编排层消化重组，多 Skill 协作时前序结果要转写成后序任务的具体指令，这是参照 Claude Code coordinator 的'禁止懒派发'纪律设计的。"

### 1.2 目标校验-重调度闭环 ★★（已写进简历）

**现状/设计**：结果目标校验，不满足携带补充指令重调度，轮次上限熔断。

**借鉴机制**：
- 校验闭环：CC coordinator 的 Verification 阶段（"证明代码能用，不是确认代码存在"）
- 熔断：CC `maxTurns` + opencode `doom_loop` 检测（harness 代码层识别重复循环，不靠模型自觉）

**落点**：轮次计数器 + 连续失败计数；同一 Skill 连续两次返回相似错误 → 降级到兜底话术而不是继续重试。

**面试话术**："熔断是双保险：轮次上限防正常任务的无限展开，连续失败计数防死循环——模型意识不到自己在打转，代码来兜底，这是 opencode doom_loop 检测给我的启发。"

### 1.3 Skill 续聊机制（演进方向）★

**借鉴机制**：三家都在向"可返聘的 worker"演进——CC 的 SendMessage/fork 续接、opencode 的 task_id resume。重述上下文的成本常常高于任务本身。

**落点**（规划中）：多轮行程修改场景（"把刚才的酒店换掉"）中，给高频 Skill 保留上一轮上下文引用，避免重新描述需求。

---

## 二、记忆系统优化（重点模块）

### 2.1 写入端

#### ① 可推导性写入门槛 ★★★

**现状**：记忆写入无过滤，存在冗余沉淀风险。

**借鉴机制**：CC `WHAT_NOT_TO_SAVE`——"Memories are constrained to context NOT derivable from the current project state"；用户明确要求存活动清单也要改道（"问什么是 surprising/non-obvious 的"）。

**落点**：
- PreferenceAgent prompt 加排除清单：行程记录里查得到的（某次航班号、某次酒店）不重复进 L1
- 编排层写入前查重：同 type 同 value 已 active 则跳过（幂等）

**面试话术**："写入前先过可推导性判断——能从行程记录和已有偏好推出的信息不存，防止记忆库膨胀成活动日志。这是 Claude Code 记忆系统的核心纪律：记忆只存推导不出来的东西。"

#### ② 三段式结构 ★★

**借鉴机制**：CC feedback 记忆正文结构：规则 + **Why:** + **How to apply:**。Why 用于边缘情况判断（"知道为什么，才能判断边缘情况而不是盲从规则"）。

**落点**：L1 schema 加 `why` / `how_to_apply` 两字段。例：
```
座位偏好 = 靠窗
Why: 用户主动声明，方便机上办公
How to apply: 值机默认勾选；若靠窗加价超阈值，询问而非自动升舱
```

**面试话术**："存 Why 是为了偏好冲突时能判断——'靠窗'和'省钱'矛盾时，模型根据原因权衡，而不是随机选一个。"

#### ③ 相对日期强制转换 ★★

**借鉴机制**：CC project 记忆规则："Always convert relative dates to absolute dates when saving (e.g., 'Thursday' → '2026-03-05')"。

**落点**：写入 L2 前转换；`start_date DATE NOT NULL` 结构兜底。

**面试话术**："记忆是写下来给一周后的会话读的，'下周三'一周后就是错的——写入时强制转绝对日期。"

#### ④ 双路提取去重 ★

**借鉴机制**：CC 主 agent 写过记忆的范围，后台 `extractMemories` 跳过（`hasMemoryWritesSince`）——"主线程写了后台补漏，主线程没写后台兜底"。

**落点**：若"会话中实时写偏好"与"会话后异步沉淀"并存，异步管线要跳过会话中已写入的条目。

### 2.2 存储端

#### ⑤ 索引/正文分离 + 常驻双闸 ★★★

**借鉴机制**：CC `MEMORY.md` 只是索引（一行 ≤150 字符指针，无 frontmatter，"Never write memory content directly into MEMORY.md"）；双闸 200 行 / 25KB（字节闸被线上"200 行 197KB"坏案例逼出来的）；超限截断并附 WARNING 教模型自修复。

**落点**：
- L3 画像拆成"常驻摘要（设 token 预算）+ 详情文件（按需读）"，别整段常驻
- 超预算触发已有的异步总结管道重压缩

**面试话术**："常驻层必须设闸。Claude Code 的索引有行数和字节双闸，字节闸是被真实坏案例逼出来的——有用户把 197KB 塞进 200 行绕过了行数限制。"

#### ⑥ 软删除 + 版本链 ★★

**落点**：L1 replace 语义执行时旧条目标记 `superseded + replaced_by`，不物理删除——支撑"偏好变更可追溯"。

### 2.3 召回端

#### ⑦ 两阶段召回 ★★★

**借鉴机制**：CC `findRelevantMemories`——扫所有记忆的 frontmatter（name+description）生成 manifest → Sonnet sideQuery + JSON schema 选 ≤5 个文件名（max_tokens 256）→ 才读正文。全程正文不进粗排。

**落点**：现有向量检索保留为粗排（对应 manifest 扫描），top-k 后加 LLM 精排挑 3 条再注入。

**面试话术**："语义相似不等于业务相关——'北京酒店'的向量检索会召回三次不同行程。我做两级：向量粗排收窄候选，LLM 精排结合当前任务意图挑选。"

#### ⑧ 召回降噪 ★★

**借鉴机制**：
- `recentTools`：正在用的工具的 API 文档不召回（对话里已有可用示例），但它的坑（gotchas）必须留
- `alreadySurfaced`：前几轮已注入的记忆不重复占额度

**落点**：本轮已调用 Skill 的相关知识不重复召回；会话内已注入条目打标记。

### 2.4 维护端

#### ⑨ autoDream 式周期整合 ★★★（当前最大缺口）

**现状**：记忆只进不治理——"优先汉庭"和"优先全季"会并存、相对日期会腐烂、条目只增不减。

**借鉴机制**：
- 三级闸门（cheapest first）：时间 ≥24h（一次 stat）→ 会话数 ≥5 → 抢锁；`DEFAULTS = {minHours:24, minSessions:5}`
- 四阶段整合：Orient（读已有防重复）→ Gather（日志 + grep 近期 transcript，不全读）→ Consolidate（合并近重复、转绝对日期、**删被推翻的事实**）→ Prune（索引压回闸内）

**落点**：异步沉淀管道加低频整合任务：冲突扫描（同 type 矛盾条目择新）+ L3 重压缩 + 索引维护。

**面试话术**："提取和整合是两级：会话后的异步提取对标 Claude Code 的 extractMemories；它之上还有 autoDream 做周期整合——24 小时加 5 个会话的闸门控制频率，负责合并近重复、消解偏好冲突、压缩索引。我的管道目前只有第一级，整合是我的下一阶段规划。"（主动认缺口比被问出来强）

#### ⑩ 用前验证 ★★

**借鉴机制**：CC `TRUSTING_RECALL`——"The memory says X exists is not the same as X exists now"，记忆提到函数先 grep。

**落点**：召回的偏好用于出方案前与本次行程约束核对（时间、城市、预算变化）；L0 下钻就是验证手段。

---

## 三、短期记忆（Session Memory 式滚动摘要）★★

**借鉴机制**（详见《Claude Code Session Memory滚动摘要机制借鉴》）：
- 惰性初始化：10000 token 以下不启用
- 双阈值更新：5000 token 增量 + 3 次工具调用
- 自然断点：上一轮无 tool_use 才切，防止孤立摘要
- 后台异步更新，不阻塞主对话
- **摘要预置给压缩**：平时滚动维护，压缩时取现成的——用平时的小成本换关键时刻的零延迟

**落点**：替换/补充现有滑动窗口：窗口 + 阈值触发的滚动摘要 + 工具输出超预算先确定性截断。

**面试话术**："会话摘要不是压缩时才现写，而是平时按阈值滚动维护，压缩触发时直接取现成摘要替换旧历史——成本从'关键时刻一次大调用'摊薄成'平时多次小调用'。"

---

## 四、RAG / 检索链路优化

#### ⑪ 召回精排与记忆同构 ★★

记忆两阶段召回的思路与 RAG 检索完全同构：向量粗排 → 精排。检索链路上的对应物：混合召回（向量+BM25/RRF）之后加 cross-encoder 或 LLM 精排。两条链路共用"粗排收窄、精排挑选"的范式，面试可以合并讲。

#### ⑫ 搜索结果输出隔离 ★★

**借鉴机制**：CC sidechain 原则——展开的内容进隔离上下文，只回传汇报；fork 的"Don't peek"（不要中途读子任务转录，否则噪音进主上下文）。

**落点**：联网搜索原始结果（可能几千 token）落 refs 文件，只有筛选后的摘要进共享上下文；需要细节时按引用回读（与 Mermaid 画布的 node_mapping/refs 同模式）。

**面试话术**："搜索原文全进上下文，几轮就爆。我按 Claude Code sidechain 的思路：原始结果独立存放，只有摘要进共享上下文，需要时按引用回读。"

---

## 五、插件化 / 披露机制优化

#### ⑬ 渐进式披露补第三要素 ★★

**现状**：已有元数据常驻（意图阶段）+ 按需展开（执行阶段）——控制"何时加载"（时间轴，启动 3 秒）。

**借鉴机制**：CC 的披露还控制"展开到哪"——全文物化到子 agent 的隔离上下文，只回传汇报（token 轴）。另有缓存联动：agent 清单嵌在工具描述里曾吃掉全车队 10.2% 的缓存创建开销，改为消息侧增量附件。

**落点**：
- 展开内容隔离（与 ⑫ 合并实施）
- Skill 元数据列表若拼在 system prompt，检查新增 Skill 对缓存前缀的影响，必要时挪到消息侧

**面试话术**："渐进式披露有两个收益轴：我做了时间轴（懒加载降启动），Claude Code 还做了 token 轴（展开内容进隔离上下文）和缓存轴（清单位置不破坏前缀）——三轴完整才是披露机制的全形。"

---

## 六、消息结构与缓存优化

#### ⑭ 消息分级瘦身（压缩五级流水线思想）★★★

**借鉴机制**：CC `queryLoop` 每轮的五级压缩，便宜的先跑：
```
工具结果预算（换内容，不调模型）→ snip（剪段）→ microcompact（按ID清）
  → 折叠（收视图）→ 最后才是 autocompact（调模型写摘要）
```
源码注释："折叠若能降到阈值下，就不值得花一次 LLM 调用，还能保住细粒度上下文。"

**落点**：上下文管理分两级即可——① 确定性截断旧工具结果（零成本）② 不够再 LLM 摘要。

**面试话术**："我的压缩是分级流水线：确定性裁剪优先于模型摘要——先截断超预算的工具输出，只有确定性手段压不下来才付 LLM 摘要的成本。"

#### ⑮ Part 化消息结构（演进方向）★

**借鉴机制**：opencode 12 种 Part 类型（判别联合，SQL 持久化 + 事件流更新）——CompactionPart 让压缩变"追加"不改写历史；StepStart/Finish 给断点提供锚点；ToolPart 类型化支持选择性剪枝。

**落点**（规划中）：消息加 `part_type` 字段（system-section / history / tool-result / dynamic / compaction-marker），压缩从重写列表改为追加标记。

#### ⑯ 缓存失效自检 ★

**借鉴机制**：CC `promptCacheBreakDetection`——每轮对 system/工具 schema/各段做指纹哈希，失效时生成 diff 定位到具体变化项（生产数据：77% 的失效来自工具 schema 悄悄变化）。

**落点**：对固定前缀各段算哈希，命中率异常时可定位变化段。轻量版即可。

---

## 七、落地优先级总表

| 优先级 | 项目 | 落地成本 | 防守价值 |
|---|---|---|---|
| ★★★ | ① 可推导性门槛 | 一段 prompt + 查重 | "怎么防记忆库膨胀" |
| ★★★ | ⑤ 索引/正文分离 + 双闸 | L3 拆分 + 预算判断 | "常驻画像怎么控成本" |
| ★★★ | ⑦ 两阶段召回 | 精排一次调用 | "向量召回污染怎么解" |
| ★★★ | ⑨ 周期整合 | 异步管道加任务 | "记忆只增不减怎么办" |
| ★★★ | ⑭ 分级压缩 | 截断 + 摘要两级 | "上下文爆炸怎么办" |
| ★★ | ②③⑥⑧⑩⑪⑫⑬ | 各几行到几十行 | 各自的深挖点 |
| ★ | ④⑮⑯、1.3 | 规划项 | 演进叙事 |

**如果只做五件事**：①⑤⑦⑨⑭——恰好覆盖"写、存、召、治、压"五个动作，构成完整闭环，每个都对应一个必考题。

---

## 八、面试总口径

> "这个项目我对照了三家开源 harness：Claude Code、opencode、deepseek-harness。借鉴遵循'取机制不取形态'——比如记忆层我借了 CC 的写入门槛、三段式结构和两阶段召回，但存储用文件化分层而不是它的目录形态，因为差旅偏好需要结构化的 append/replace 语义；编排层我借了 coordinator 的综合纪律和 sidechain 的输出隔离，但路由保持确定性，因为意图集合封闭；压缩我借了'便宜操作先跑'的分级流水线。判断标准始终是：这个机制解决的问题在我的场景是否存在。"
