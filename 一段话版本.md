"这个差旅助手是我从架构到核心模块全程参与的多 Agent 系统。整体是意图-编排两级解耦：IntentionAgent 只负责解析意图、产出结构化调度计划，不做执行；OrchestrationAgent 按优先级分组调度六个子智能体，覆盖记忆查询、偏好管理、联网搜索、企业知识库和行程规划，并且对子智能体的返回结果做目标校验，不满足就携带补充指令重新调度，配轮次上限熔断兜底。这一层我们没有用任何 Agent 框架，是参考 Claude Code 的 Task 式派发手写的一套轻量编排。记忆系统分短期和长期：短期是滑动窗口加阈值触发的滚动摘要，长期是四层语义金字塔——原始对话存证据、原子事实存偏好、行程聚场景、画像做总结，检索时画像定向、逐层下钻，写入有可推导性门槛，召回是向量粗排加 LLM 精排两级，偏好记忆准确率做到 95%。检索是双源管线：企业知识库走 Milvus 加 BGE-m3，问答准确率 95%；联网搜索前置意图路由，多跳走 ReAct、事实单跳直出、天气汇率走专用 API。子智能体做成插件化架构，启动只加载元数据，执行时才按需展开，启动优化到 3 秒。最终意图识别准确率从初版的 65% 提到 90% 以上。"


① Agent 消息结构与 KV Cache 优化
这个项目解决的是 Agent 链路大模型调用成本高的问题。Agent 每轮调用都会发送 System 提示、历史和工具结果，大模型侧会缓存上次推理的 KV 中间状态，前缀一致就能复用、跳过重复计算——但我梳理发现各链路把时间、位置、检索结果这些每轮都在变的内容混进了消息前缀，导致缓存大面积失效。我的方案是把消息结构重构成三段：固定 System、只增不改的稳定历史、以及集中存放动态内容的动态区，动态内容每轮先删后建，显式缓存打两个断点——一个在 System，一个随历史增长移动；再配套一个递归摘要模块解决长对话历史膨胀，摘要的生命周期与缓存对齐。最后把这些逻辑沉淀成通用模块，接入了 8 条链路。效果：WebSearch 线上实验显式缓存命中率 56.7%，输入成本降约 40%；RAG Agentic 命中率 82%，成本率压到 30%。


② Memory-Skill 记忆抽取服务
这个项目解决的是 Agent 执行经验无法跨会话复用的问题——同类任务每次都要重新试错。我设计并上线了一条从任务轨迹自动沉淀可复用技能的全链路服务，分五段：轨迹分段、意图识别、质量校验、技能生成、增量决策。几个关键设计：意图识别用正则加 LLM 两级闸门，先挡掉无关会话省成本；质量校验用两段式 Rubrics——模型先针对任务自出考卷再逐条判卷，坏轨迹直接拦截；生成环节骨架、示例、排障三段并行，必填段重试兜底，保证链路零中断；增量更新用三路并行检索防重复，配合局部序号替代真实 ID、定向字段覆盖这些契约防 LLM 幻觉改坏存量技能。服务已按客户灰度上线，全链路基于 qwen-plus 通用模型，质量收益完全来自管线策略而非模型训练。