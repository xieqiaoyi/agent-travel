# 差旅助手——面试官讲解稿（口语版）

> 用法：第一节是开场白（30 秒/2 分钟两档），中间按模块给"主讲内容 + 预判追问 + 短答"，最后是收口和红线
> 原则：先说做什么、再说为什么这么做、最后说效果；每个模块留一个钩子等面试官追问，不要一次倒完

---

## 一、开场（必背）

### 30 秒版（自我介绍里带项目）

> "我做过一个差旅出行助手，是意图-编排两级解耦的多 Agent 系统。没有用 Agent 框架，手写了一套轻量编排层：IntentionAgent 只做意图解析产出调度计划，OrchestrationAgent 按优先级调度六个子智能体，覆盖记忆、搜索、知识库和行程规划，还能对子任务结果做校验重调度。我主要负责整体架构和记忆、检索两个核心模块。"

### 2 分钟版（"介绍一下这个项目"）

> "这是一个给企业用户提供个性化行程规划的多 Agent 系统。
>
> 先说业务：用户以前要在多个平台查攻略、查政策、订酒店，一次规划两三个小时，而且系统完全不记得他的偏好，每次都要重新说。我们要做的就是把这些收敛到一次对话里。
>
> 架构上做了三个关键决策。**第一，不用 Agent 框架，手写轻量编排层**——我对照过 Claude Code、opencode 这些开源 harness，差旅任务意图集合封闭、流程短，框架的抽象收益是负的，所以自己写：单主循环、agent 用配置声明、派发-汇报模式。**第二，意图和编排两级解耦**——IntentionAgent 只产出结构化的调度计划，不做执行，保证意图判断可以被单独评测和降级；执行全部交给 OrchestrationAgent 按优先级分组调度。**第三，记忆和检索做成独立分层模块**，不跟编排逻辑耦合。
>
> 效果上，意图识别准确率从初版关键词匹配的 65% 提到 90% 以上，用户偏好记忆准确率 95%，知识库问答 95%。"

（停顿，等追问。不要主动往下展开。）

---

## 二、模块讲解（按追问方向选用）

### 模块 1：多 Agent 架构

**主讲（1 分钟）**：

> "六个子智能体覆盖记忆查询、偏好管理、联网搜索、企业知识库、行程规划。交互是 Task 式派发：编排层把任务打包派出去，子智能体在独立上下文里跑完，只回传结果，中间过程不进主上下文。派发前编排层会把前序结果消化成具体指令，不原样转发；结果回来先做目标校验，不满足就携带补充指令重新调度，配轮次上限熔断防死循环。"

**预判追问**：

| 问题 | 短答 |
|---|---|
| 为什么不用 LangGraph / AgentScope？ | "任务短周期、意图集合封闭，图编排的价值在复杂状态机，我们是线性流程加确定路由，框架的抽象是纯成本。我对照过三家开源 harness 都是手写编排，这是业界在轻量场景的共识。" |
| 子智能体之间怎么通信？ | "不直接通信，全部经过编排层中转——保持星型拓扑，任何一步可审计可熔断。这跟 Claude Code 普通 subagent 的设计一致，只有长周期开放任务才需要 swarm 式互联。" |
| 路由为什么是确定性的，不用模型自主选？ | "意图就六大类、可穷举，正则加轻量分类能到 90% 以上且零误派；模型自主路由的收益在开放工具集场景，我们是封闭集合，确定性换可控性更值。" |

### 模块 2：记忆系统（最可能被深挖）

**主讲（1 分钟）**：

> "记忆分短期和长期。短期是会话内滑动窗口加阈值触发的滚动摘要——这个摘要不是压缩时才现写，而是平时按 token 阈值滚动维护，上下文要压缩时直接取现成的，关键时刻零额外延迟。
>
> 长期是四层语义金字塔：L0 原始对话 append-only 存证据，L1 是原子事实比如偏好和常住地，L2 按行程聚场景，L3 是异步总结出的稳定画像。检索时 L3 定向、逐层下钻到 L1 取值、必要时回 L0 核验，所以既可追溯又省上下文。
>
> 写入由 PreferenceAgent 负责，它输出结构化的追加/覆盖动作，由编排层校验执行，不让模型直接改存储；带'这次'的临时约束会分流到 L2 场景，不污染长期偏好。存储是文件化分层，无数据库依赖，整个系统无状态可水平扩容。"

**预判追问**：

| 问题 | 短答 |
|---|---|
| L1 和 L3 不重复吗？ | "L1 是给程序消费的结构化字段，L3 是给模型定向的整体总结，而且 L3 能归纳出 L1 没有的规律——比如'客户指定时可接受非偏好酒店'，这从多次行程里总结出来，任何单条 L1 表达不了。" |
| 怎么防止记忆库膨胀？ | "写入前过可推导性门槛——行程记录里查得到的不重复存。这个思路参考 Claude Code，它的记忆系统核心纪律就是只存推导不出来的东西。" |
| 偏好冲突怎么办（先说汉庭后说全季）？ | "PreferenceAgent 区分 append 型和 replace 型；真出现语义冲突靠周期性整合任务扫描消解——提取和整合是两级，这块我参照的是 Claude Code 的 autoDream 机制，目前在规划落地。"（主动认缺口） |
| 为什么不用数据库？ | "用户记忆是 KB 级半结构化文本，访问模式是整份读给模型，不是高频结构化查询；文件加 append-only 天然可审计可版本化，而且无数据库让整个系统无状态，扩容不用分片。"（追问 Milvus 就答：那是知识库的检索资产，和用户记忆层刻意分离） |
| 记忆怎么召回？ | "两级：向量粗排收窄候选，LLM 精排结合当前任务意图挑最相关的——语义相似不等于业务相关，纯向量会把三次不同行程的酒店都召回。" |

### 模块 3：RAG 与搜索

**主讲（45 秒）**：

> "双源管线：企业知识库走 Milvus 加 BGE-m3，滑动窗口切分，混合索引，问答准确率 95%；联网搜索前置意图路由——多跳问题走 ReAct 循环，事实型单跳直出，天气、汇率、限行这类有专用 API，攻略类走垂直搜索，最后通用搜索兜底。每一路都有降级策略，任何一路挂了不影响整体回答。"

**预判追问**：

| 问题 | 短答 |
|---|---|
| 为什么多跳要单独路由？ | "单跳和多跳的执行成本差一个量级，全走 ReAct 是浪费，全走单跳又答不了'对比类'问题。意图路由让成本匹配问题复杂度。" |
| 搜索结果怎么进上下文？ | "原始结果独立存放，只有筛选后的摘要进共享上下文，需要细节按引用回读——不然一次搜索几千 token，几轮就爆。" |

### 模块 4：插件化与渐进式披露

**主讲（30 秒）**：

> "子智能体做成 Skill 插件：注册表启动只扫描元数据不加载实现，首次调用才初始化并缓存，启动从十几秒降到 3 秒。意图阶段只给模型看 Skill 的一句话描述，执行阶段才加载详细指令——元数据常驻、正文按需。"

**预判追问**：被问披露机制细节时补一句："披露有两个收益轴，我做的是时间轴；Claude Code 还做 token 轴——展开的内容进子任务隔离上下文只回传摘要。这是我在持续补的方向。"

### 模块 5：上下文与缓存（简历有这条才讲）

**主讲（45 秒）**：

> "多轮链路里动态内容混在消息里会破坏 KV Cache 前缀。我把消息架构拆成四块：固定前缀放系统指令和 Skill 元数据，整个会话不变；对话历史 append-only 只在尾部推进断点；日期、用户画像这类易变内容全部后置到动态区；历史字节永不改写，压缩以追加摘要方式进行。对照过 Claude Code 源码，它用动态边界标记切静态段、易变内容放 user 消息侧，思路完全同构。"

**预判追问**：断点数量被质疑时答："Claude Code 消息级用单断点，因为了解服务端按断点保护 KV 页、多断点浪费显存；在只提供隐式前缀缓存的服务商上，双断点显式声明两段共享边界收益更确定——断点数量要看服务商接口形态选。"

---

## 三、收口（被问"设计哲学/最大收获"时用）

> "做完这个项目我最大的收获是建立了一个选型判据：**机制的显式化程度要匹配任务的可预知度**。意图封闭就用确定性路由，任务可分解就用轻量派发，只有开放长周期任务才上复杂协作——这条原则是我对照三家开源 harness 总结出来的，也是我后续做所有架构决策的默认尺子。"

---

## 四、红线（绝对不说 / 必须诚实的三点）

1. **不说"参考了 AgentScope 的 XX 实现"**——口径统一为手写轻量编排，借鉴对象是 Claude Code / opencode / dsh 的设计思想
2. **周期整合（autoDream 对标）必须说"规划中"**——这是唯一没落地的 ★★★ 项，主动说比被挖出来好
3. **指标只有三个是真的**：65%→90% 意图准确率、95% 偏好记忆、95% 知识库问答。其他数字（响应时间、命中率）被追问就说"离线评测口径"，不要现场编新数字

---

## 五、钩子清单（主动埋，引导追问到你准备好的地方）

| 埋在哪 | 钩子 | 引向 |
|---|---|---|
| 开场结尾 | "记忆模块我花的心思最多" | 四层金字塔全套 |
| 架构段 | "对照过三家开源 harness" | 架构对比、无 SDK 论证 |
| 记忆段 | "写入有门槛、召回有精排" | 可推导性、两阶段召回 |
| 缓存段 | "对照过 Claude Code 源码，发现一个断点数量的差异" | 单断点机制，展示源码深度 |
